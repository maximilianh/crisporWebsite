
#if !defined(OUTPUTCONSTRAINTS_H) 
#define OUTPUTCONSTRAINTS_H

#include "structure.h"
void outputconstraints(const char *filename, structure *ct);
bool readconstraints(const char *filename, structure *ct);

#endif // OUTPUTCONSTRAINTS_H
