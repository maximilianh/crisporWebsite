# Test configuration for running tests via the gitlab-runner (Continuous Integration)
# (This is a bash file that is sourced from test-runner.sh when the TEST_CONFIG environment variable is set.)

TEST_OPTS=QDC  #(Quiet, DumpErrors CleanExit)

EXCLUDE_TESTS+='
  bifold_sav_plot			bifold-smp_sav_plot
  ShapeKnots_window_size_option		ShapeKnots-smp_window_size_option
' # End of EXCLUDE_TESTS
