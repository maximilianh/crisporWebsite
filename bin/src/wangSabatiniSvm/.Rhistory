install.package("e1071");
install.packages("e1071");
install.packages("e1071");
install.packages("e1071")
?install.packages
?install.packages
install.packages("e1071")
system("ls -ld /usr/local /usr/local/lib /usr/local/lib/libtcl*")
install.packages("limma")
source("http://bioconductor.org/biocLite.R")
biocLite("limma")
library(e1017)
library(e1071)
RMA=read.delim("training.txt", header = TRUE, row.names = 1)
negative=-1.6
myrows=c(6:85)
totalclasses=array(1,dim(RMA)[1])
totalclasses[which(negative>RMA$l2fc)]=-1
predictor<-svm( as.matrix(RMA[,myrows]), as.factor(totalclasses),probability=T)
library(e1071)
predictor<-svm( as.matrix(RMA[,myrows]), as.factor(totalclasses),probability=T)
set1=read.delim(args[1], header = TRUE, row.names = 1)
library(limma)
set1=read.delim(args[1], header = TRUE, row.names = 1)
set1=read.delim("input.txt", header = TRUE, row.names = 1)
head(set1)
preds = slot(predict(predictor, as.matrix(set1),probability=T)
;
preds <- slot(predict(predictor, as.matrix(set1),probability=T))
preds <- slot(predict(predictor, as.matrix(set1),probability=T))
preds <- predict(predictor, as.matrix(set1),probability=T)
preds
?preds
type(preds)
class(preds)
info(preds)
preds$probabilities
attr(preds)
attr(preds, "probabilities")
types(attr(preds, "probabilities"))
class(attr(preds, "probabilities"))
attr(preds, "probabilities")[,2[
attr(preds, "probabilities")[,2]
;
attr(preds, "probabilities")[,2]
system("cat scorer.R")
set1.pred = attr(preds, "probabilities")
set1.pred
attr(preds, "probabilities")
attr(preds, "probabilities")[,2]
attr(preds, "probabilities")
is.vector(as.factor(totalclasses))
is.vector(as.matrix(set1))
source("scorer.R")
